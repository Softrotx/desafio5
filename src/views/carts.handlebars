<div id="carrito">
    {{#each products}}
    <div class="producto">
        <h3>{{productId.title}}</h3>
        <p id="productId">{{productId._id}}</p>
        <p> Precio: {{productId.price}} CLP</p>
    {{/each}}
    </div>

</div>